# This file is for running the application locally

services:

  web:
    image: biodiversitydata-se/hosted-portal-lab
    build: 
      context: .
      dockerfile: dev.Dockerfile
    ports:
      - 4000:4000
      - 35729:35729
    volumes:
      - .:/site
